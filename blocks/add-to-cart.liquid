

{%- doc -%}
  This block is used to display the add to cart button.
  Intended for product-form.liquid block.

  @param {string} can_add_to_cart - Whether the product can be added to the cart
  @param {string} add_to_cart_text - The text of the add to cart button
{%- enddoc -%}

{% liquid
  assign scoped_class = block.settings.style_class | append: '--' | append: block.id
  assign class = 'add-to-cart-button ' | append: block.settings.style_class | append: ' ' | append: scoped_class
  assign id = 'BuyButtons-ProductSubmitButton-' | append: block.id

  assign show_icon = can_add_to_cart
  if block.settings.hide_icon
    assign show_icon = false
  endif

  assign button_text = add_to_cart_text
  if can_add_to_cart and block.settings.custom_label != blank
    assign button_text = block.settings.custom_label
  endif
%}
{% render 'button-custom-styles', block_settings: block.settings, block_id: block.id %}
{% render 'add-to-cart-button-styles' %}
{% render 'fly-to-cart-styles' %}

{% if block.settings.border_radius_override != blank %}
  {% style %}
    .{{ scoped_class }} {
      border-radius: {{ block.settings.border_radius_override | remove: 'px' | strip }}px;
    }
  {% endstyle %}
{% endif %}

<span
  {{ block.shopify_attributes }}
  style="--add-to-cart-font-case: {{ settings.button_text_case }};"
>
  {% render 'add-to-cart-button',
    id: id,
    class: class,
    can_add_to_cart: can_add_to_cart,
    show_icon: show_icon,
    product: closest.product,
    add_to_cart_text: button_text,
    data_testid: 'standalone-add-to-cart'
  %}
</span>

{% schema %}
{
  "name": "t:names.add_to_cart",
  "tag": null,
  "settings": [
    {
      "type": "text",
      "id": "custom_label",
      "label": "Button label override",
      "info": "Leave blank to use the default \"Add to cart\" text. Only applies when the product can be purchased — sold out / unavailable states always use their own label."
    },
    {
      "type": "checkbox",
      "id": "hide_icon",
      "label": "Hide cart icon",
      "default": false
    },
    {
      "type": "text",
      "id": "border_radius_override",
      "label": "Border radius override",
      "placeholder": "e.g. 8 or 999",
      "info": "Leave blank to use the theme's default button shape (Theme settings > Buttons)."
    },
    {
      "type": "select",
      "id": "style_class",
      "label": "t:settings.style",
      "options": [
        {
          "value": "button",
          "label": "t:options.primary"
        },
        {
          "value": "button-secondary",
          "label": "t:options.secondary"
        },
        {
          "value": "button-custom",
          "label": "t:options.custom"
        }
      ],
      "default": "button-secondary",
      "info": "t:info.edit_button_style_in_theme_settings"
    },
    {
      "type": "header",
      "content": "t:content.button_colors",
      "visible_if": "{{ block.settings.style_class == \"button-custom\" }}"
    },
    {
      "type": "color",
      "id": "custom_button_background",
      "label": "t:settings.background",
      "default": "{{ settings.color_palette.foreground }}",
      "alpha": true,
      "visible_if": "{{ block.settings.style_class == 'button-custom' }}"
    },
    {
      "type": "color",
      "id": "custom_button_text",
      "label": "t:settings.text",
      "default": "{{ settings.color_palette.background }}",
      "alpha": true,
      "visible_if": "{{ block.settings.style_class == 'button-custom' }}"
    },
    {
      "type": "color",
      "id": "custom_button_border",
      "label": "t:settings.borders",
      "default": "{{ settings.color_palette.background }}",
      "alpha": true,
      "visible_if": "{{ block.settings.style_class == 'button-custom' }}"
    }
  ]
}
{% endschema %}
